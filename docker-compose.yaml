# docker-compose.yaml
version: '3.3'

services:
  nexus:
    image: sonatype/nexus3:latest
    container_name: nexus
    restart: always
    ports:
      - "8088:8081"
    volumes:
      - /path/to/nexus/data:/nexus-data
